---
phase: 02-database-schema
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docker-compose.yml
  - .env
autonomous: true
requirements:
  - DB-03
user_setup:
  - tool: Docker Desktop
    why: PostgreSQL and pgAdmin containers need Docker runtime
    check: docker info
must_haves:
  truths:
    - Developer can start PostgreSQL locally with docker compose up
    - pgAdmin management UI is accessible on localhost
  artifacts:
    - path: docker-compose.yml
      provides: PostgreSQL 16 and pgAdmin 4 services
      contains: services postgres, services pgadmin, volumes postgres_data, volumes pgadmin_data
    - path: .env
      provides: DATABASE_URL for local PostgreSQL
      contains: DATABASE_URL=postgresql://postgres:postgres@localhost:5432/river
  key_links:
    - from: .env
      to: docker-compose.yml postgres service
      pattern: POSTGRES_USER: postgres, POSTGRES_PASSWORD: postgres, POSTGRES_DB: river
---

<objective>
Set up local PostgreSQL infrastructure with Docker Compose and a root environment file.

Purpose: Provide the runtime database environment needed for Drizzle schema development and migration testing.
Output: `docker-compose.yml` with postgres + pgAdmin services, and `.env` with `DATABASE_URL`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-database-schema/02-CONTEXT.md
@.planning/phases/02-database-schema/02-RESEARCH.md
@.planning/REQUIREMENTS.md

# Locked decisions affecting this plan:
# D-01: Include PostgreSQL and pgAdmin in docker-compose.yml
# D-05: .env lives at repository root
# D-06: Connection string variable is DATABASE_URL
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create docker-compose.yml with PostgreSQL and pgAdmin</name>
  <files>docker-compose.yml</files>
  <read_first>.planning/phases/02-database-schema/02-CONTEXT.md</read_first>
  <action>
    Create `/Users/lionad/Github/Lionad-Morotar/project-river/docker-compose.yml` with exactly these services:

    - `postgres`:
      - image: `postgres:16`
      - container_name: `river_postgres`
      - restart: `unless-stopped`
      - environment:
        - POSTGRES_USER: `postgres`
        - POSTGRES_PASSWORD: `postgres`
        - POSTGRES_DB: `river`
      - ports: `["5432:5432"]`
      - volumes: `postgres_data:/var/lib/postgresql/data`

    - `pgadmin`:
      - image: `dpage/pgadmin4:latest`
      - container_name: `river_pgadmin`
      - restart: `unless-stopped`
      - depends_on: `[postgres]`
      - environment:
        - PGADMIN_DEFAULT_EMAIL: `admin@localhost.com`
        - PGADMIN_DEFAULT_PASSWORD: `admin`
      - ports: `["5050:80"]`
      - volumes: `pgadmin_data:/var/lib/pgadmin`

    - top-level volumes: `postgres_data`, `pgadmin_data`

    Per D-01, pgAdmin must be included for local database management.
  </action>
  <verify>
    <automated>test -f /Users/lionad/Github/Lionad-Morotar/project-river/docker-compose.yml && grep -q 'image: postgres:16' /Users/lionad/Github/Lionad-Morotar/project-river/docker-compose.yml && grep -q 'image: dpage/pgadmin4:latest' /Users/lionad/Github/Lionad-Morotar/project-river/docker-compose.yml && echo 'OK'</automated>
  </verify>
  <done>docker-compose.yml exists and defines postgres:16 and dpage/pgadmin4:latest services with correct ports and volumes.</done>
  <acceptance_criteria>
    - `docker-compose.yml` contains `services:` with `postgres:` and `pgadmin:`
    - `postgres` service has `image: postgres:16`, `container_name: river_postgres`, `POSTGRES_DB: river`, port `5432:5432`
    - `pgadmin` service has `image: dpage/pgadmin4:latest`, `container_name: river_pgadmin`, port `5050:80`, `depends_on: [postgres]`
    - Top-level `volumes:` declares `postgres_data:` and `pgadmin_data:`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Create root .env with DATABASE_URL</name>
  <files>.env</files>
  <read_first>.planning/phases/02-database-schema/02-CONTEXT.md</read_first>
  <action>
    Create `/Users/lionad/Github/Lionad-Morotar/project-river/.env` containing exactly this line (and no other content):

    ```
    DATABASE_URL=postgresql://postgres:postgres@localhost:5432/river
    ```

    Per D-05 and D-06, the `.env` file lives at the repository root and the connection string variable is named `DATABASE_URL`.
  </action>
  <verify>
    <automated>test -f /Users/lionad/Github/Lionad-Morotar/project-river/.env && grep -q '^DATABASE_URL=postgresql://postgres:postgres@localhost:5432/river$' /Users/lionad/Github/Lionad-Morotar/project-river/.env && echo 'OK'</automated>
  </verify>
  <done>Root `.env` exists and contains `DATABASE_URL=postgresql://postgres:postgres@localhost:5432/river`.</done>
  <acceptance_criteria>
    - `.env` file exists at repository root
    - `.env` contains exactly `DATABASE_URL=postgresql://postgres:postgres@localhost:5432/river`
    - No trailing slash or additional query params on the URL
  </acceptance_criteria>
</task>

</tasks>

<verification>
After both tasks complete:
1. `docker compose config` (or `docker-compose config`) exits 0 and parses the file without errors.
2. Root `.env` is readable and contains the exact `DATABASE_URL`.
</verification>

<success_criteria>
- `docker-compose.yml` defines runnable PostgreSQL 16 and pgAdmin 4 services.
- `.env` at repository root supplies `DATABASE_URL=postgresql://postgres:postgres@localhost:5432/river`.
</success_criteria>

<output>
After completion, create `.planning/phases/02-database-schema/02-01-SUMMARY.md`
</output>

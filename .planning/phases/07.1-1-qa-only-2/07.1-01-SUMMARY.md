---
phase: 07.1
plan: 01
subsystem: packages/db
tags: [review, qa, database, drizzle]
dependency_graph:
  requires: []
  provides: ["07.1-01-db-review.md"]
  affects: [packages/db]
tech-stack:
  added: []
  patterns: []
key-files:
  created:
    - .planning/phases/07.1-1-qa-only-2/07.1-01-db-review.md
  modified: []
decisions: []
metrics:
  duration: "~6 minutes"
  completed_date: "2026-04-09"
---

# Phase 07.1 Plan 01: packages/db Review Summary

**One-liner:** Structured QA review of the database package covering schema design, client initialization, Drizzle migrations, and package configuration.

## Completed Tasks

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Review packages/db source and migrations | 2df5397 | .planning/phases/07.1-1-qa-only-2/07.1-01-db-review.md |
| 2 | Record build/test verification for packages/db | 52f4162 | .planning/phases/07.1-1-qa-only-2/07.1-01-db-review.md |

## Artifacts Produced

- `.planning/phases/07.1-1-qa-only-2/07.1-01-db-review.md`
  - Sections: Summary, Code Quality, Type Safety, Test Coverage, Architectural Consistency, Findings (critical/non-critical), Recommendations, Verification.

## Key Findings

- **Critical (3):**
  1. `client.ts` eagerly instantiates `Pool` at module load with `process.env.DATABASE_URL` — crashes on import if missing and blocks testing.
  2. Zero test files and no `test` script in `package.json`.
  3. `drizzle.config.ts` uses a non-null assertion on `DATABASE_URL`, masking missing config from compile-time checks.

- **Non-Critical (6):**
  1. Inconsistent date modes between `commits.committerDate` (`mode: 'date'`) and stats tables (`mode: 'string'`).
  2. Table name `sum_day` is conceptually inconsistent with `daily_stats`.
  3. `daily_stats` lacks a unique constraint on `(project_id, date, contributor)`.
  4. No `tsconfig.json` in `packages/db`.
  5. `package.json` defines redundant `main` alongside `exports`.
  6. Missing composite index on `daily_stats(project_id, contributor, date)`.

## Deviations from Plan

None — plan executed exactly as written.

## Known Stubs

None.

## Self-Check: PASSED

- [x] `07.1-01-db-review.md` exists
- [x] `07.1-01-db-review.md` references core.ts, stats.ts, client.ts, and migrations by name
- [x] Findings are categorized as critical and non-critical
- [x] Verification section is present
- [x] Commits 2df5397 and 52f4162 exist in git log

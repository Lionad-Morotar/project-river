---
phase: 07.1
plan: 04
type: execute
wave: 2
depends_on:
  - "07.1-01"
  - "07.1-02"
  - "07.1-03"
files_modified:
  - packages/pipeline/src/cli.ts
  - packages/pipeline/src/db/analyze.ts
  - packages/pipeline/tsconfig.json
  - apps/web/server/api/projects/[id]/daily.get.test.ts
  - apps/web/server/api/projects/[id]/monthly.get.test.ts
autonomous: true
requirements:
  - PIPE-03
  - PIPE-04
  - API-01
  - API-02
must_haves:
  truths:
    - "Pipeline lint passes with no blocking errors"
    - "Web API route tests pass when DATABASE_URL is absent"
    - "No functional regressions introduced by fixes"
  artifacts:
    - path: packages/pipeline/src/cli.ts
      provides: "Fixed process global import"
    - path: packages/pipeline/src/db/analyze.ts
      provides: "Removed unused sql import"
    - path: packages/pipeline/tsconfig.json
      provides: "Sorted JSON keys per lint"
    - path: apps/web/server/api/projects/[id]/daily.get.test.ts
      provides: "Skips DB-dependent 404 test when DATABASE_URL missing"
    - path: apps/web/server/api/projects/[id]/monthly.get.test.ts
      provides: "Skips DB-dependent 404 test when DATABASE_URL missing"
  key_links:
    - from: "apps/web/server/api/projects/[id]/daily.get.test.ts"
      to: "DATABASE_URL presence"
      via: "hasDb guard"
      pattern: "if (!hasDb)"
    - from: "packages/pipeline/src/cli.ts"
      to: "lint pass"
      via: "import process from 'node:process'"
      pattern: "node/process"
---

<objective>
Auto-fix critical/blocking issues identified during the module reviews: pipeline lint failures and brittle web API tests that fail without DATABASE_URL.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07.1-1-qa-only-2/07.1-02-pipeline-review.md
@.planning/phases/07.1-1-qa-only-2/07.1-03-web-review.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix pipeline lint blockers</name>
  <files>
    packages/pipeline/src/cli.ts,
    packages/pipeline/src/db/analyze.ts,
    packages/pipeline/tsconfig.json
  </files>
  <read_first>
    packages/pipeline/src/cli.ts,
    packages/pipeline/src/db/analyze.ts,
    packages/pipeline/tsconfig.json
  </read_first>
  <action>
    Apply the following safe, localized fixes:
    1. In `packages/pipeline/src/cli.ts`, add `import process from 'node:process'` at the top of the file to resolve `node/prefer-global/process`.
    2. In `packages/pipeline/src/db/analyze.ts`, remove the unused `sql` import from `drizzle-orm` (keep `eq` and other used imports).
    3. In `packages/pipeline/tsconfig.json`, move the `paths` key before `strict` to satisfy `jsonc/sort-keys`.
    Do not change any other logic.
  </action>
  <acceptance_criteria>
    - `pnpm --filter @project-river/pipeline lint` exits with code 0.
    - No functional behavior changes (only imports/style).
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @project-river/pipeline lint</automated>
  </verify>
  <done>Pipeline lint passes with no errors.</done>
</task>

<task type="auto">
  <name>Task 2: Fix web API route test brittleness without DATABASE_URL</name>
  <files>
    apps/web/server/api/projects/[id]/daily.get.test.ts,
    apps/web/server/api/projects/[id]/monthly.get.test.ts
  </files>
  <read_first>
    apps/web/server/api/projects/[id]/daily.get.test.ts,
    apps/web/server/api/projects/[id]/monthly.get.test.ts
  </read_first>
  <action>
    Both test files already define `const hasDb = !!process.env.DATABASE_URL`. Make the 404 tests conditional so they skip when `hasDb` is false, matching the pattern used by DB-dependent integration tests elsewhere in the repo. Keep the 400 validation tests unconditional because they do not require a database connection.
    - In `daily.get.test.ts`, update the "returns 404 for nonexistent project" test to skip when `!hasDb`.
    - In `monthly.get.test.ts`, update the "returns 404 for missing project" test to skip when `!hasDb`.
    Prefer `const testOrSkip = hasDb ? it : it.skip` followed by `testOrSkip('...', async () => { ... })` for clarity.
  </action>
  <acceptance_criteria>
    - `pnpm --filter @project-river/web test` passes when `DATABASE_URL` is unset.
    - The 400 validation tests still execute regardless of DB presence.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @project-river/web test</automated>
  </verify>
  <done>Web tests pass locally without DATABASE_URL.</done>
</task>

<task type="auto">
  <name>Task 3: Verify no regressions in affected packages</name>
  <files>—</files>
  <read_first>packages/pipeline/package.json, apps/web/package.json</read_first>
  <action>
    Run `pnpm --filter @project-river/pipeline test` and confirm all tests pass (integration tests will skip without DB, which is expected). Then run `pnpm --filter @project-river/web build` to confirm the build still succeeds. Record the results in a brief note within the plan output or SUMMARY.
  </action>
  <acceptance_criteria>
    - Pipeline tests pass (or skip gracefully).
    - Web build succeeds.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @project-river/pipeline test && pnpm --filter @project-river/web build</automated>
  </verify>
  <done>All affected package tests and builds pass.</done>
</task>

</tasks>

<verification>
- Pipeline lint exits 0.
- Web tests exit 0 without DATABASE_URL.
- Build commands exit 0.
</verification>

<success_criteria>
Critical issues from reviews are fixed, lint and tests are green, and no regressions were introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/07.1-1-qa-only-2/07.1-04-SUMMARY.md`
</output>

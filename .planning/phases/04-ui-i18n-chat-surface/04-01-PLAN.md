---
phase: 04-ui-i18n-chat-surface
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/package.json
  - apps/web/i18n/locales/zh-CN.ts
  - apps/web/i18n/locales/en.ts
  - apps/web/app/components/AgentToolCard.vue
  - apps/web/app/components/AgentChat.vue
  - apps/web/app/pages/projects/[id]/index.vue
autonomous: true
requirements:
  - UI-05
  - UI-06
  - UI-07
  - UI-08
  - I18N-01

must_haves:
  truths:
    - "用户点击右下 FAB 按钮，USlideover drawer 从右侧 slide in，宽度 40vw（mobile 全屏）"
    - "点击 drawer header 的 minimize 按钮或 Esc/overlay，drawer 缩小回 FAB，messages/tool-cards 状态保留"
    - "5 个 chip questions 中英双语，点击自动填充输入框并提交"
    - "SSE streaming token 实时追加到 assistant 消息气泡"
    - "tool-call 卡片默认折叠，点击 header 展开显示 input/output JSON + duration + isError badge"
    - "10 个 UI States 全部有明确视觉反馈（idle/streaming/tool-calling/stream-mid-error/abort/rate-limit/cost-cap/input-too-long/api-key-missing/empty-result）"
    - "切换 locale 时 drawer 内所有文案（title/chip/error/toolcard 标签）同步变化"
  artifacts:
    - path: "apps/web/package.json"
      provides: "@microsoft/fetch-event-source exact dependency"
      contains: "@microsoft/fetch-event-source"
    - path: "apps/web/i18n/locales/zh-CN.ts"
      provides: "12 个 agent.* 中文 key"
      contains: "agent: {"
    - path: "apps/web/i18n/locales/en.ts"
      provides: "12 个 agent.* 英文 key"
      contains: "agent: {"
    - path: "apps/web/app/components/AgentToolCard.vue"
      provides: "折叠/展开 tool-call 卡片"
      exports: ["default"]
      min_lines: 60
    - path: "apps/web/app/components/AgentChat.vue"
      provides: "FAB + USlideover + 状态机 + chip + 消息列表 + 输入 + 错误覆盖"
      exports: ["default"]
      min_lines: 250
    - path: "apps/web/app/pages/projects/[id]/index.vue"
      provides: "AgentChat 挂载点"
      contains: "<AgentChat"
  key_links:
    - from: "AgentChat.vue"
      to: "AgentToolCard.vue"
      via: "import AgentToolCard"
      pattern: "import AgentToolCard"
    - from: "AgentChat.vue"
      to: "/api/projects/[id]/agent"
      via: "fetchEventSource POST"
      pattern: "fetchEventSource.*api/projects.*agent"
    - from: "AgentChat.vue"
      to: "i18n"
      via: "useI18n()"
      pattern: "const \\{ t \\} = useI18n\\(\\)"
    - from: "index.vue"
      to: "AgentChat.vue"
      via: "template mount"
      pattern: "<AgentChat.*project-id"
---

<objective>
构建 Phase 4 核心 UI 层：AgentChat.vue（FAB + USlideover drawer + 10 状态机 + SSE 消费）+ AgentToolCard.vue（折叠卡片）+ 12 个 i18n key 中英双语 + 页面挂载。

Purpose: 这是用户与 Agent 交互的唯一入口。Phase 3 的 SSE 路由在此被消费，流式 token、tool-call cards、错误状态全部可视化。

Output: 5 个文件修改/创建，AgentChat 组件可在项目详情页打开、提问、接收流式回答、展开 tool cards。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-ui-i18n-chat-surface/04-CONTEXT.md
@.planning/phases/04-ui-i18n-chat-surface/04-UI-SPEC.md
@.planning/phases/03-agent-engine-and-route/03-01-SUMMARY.md
@.planning/phases/03-agent-engine-and-route/03-02-SUMMARY.md

<interfaces>
<!-- Phase 3 SSE event 合约（consumer 需要） -->

```typescript
// SSE event types from POST /api/projects/[id]/agent
type SseEvent =
  | { type: 'text'; token: string }
  | { type: 'tool-call'; id: string; name: string; args: Record<string, unknown> }
  | { type: 'tool-result'; id: string; name: string; result: unknown; isError: boolean }
  | { type: 'done' }
  | { type: 'error'; message: string }
```

<!-- 现有 i18n 结构（尾部追加 agent 命名空间） -->

From apps/web/i18n/locales/zh-CN.ts (line 251 ends):
```typescript
export default defineI18nLocale(() => ({
  // ... existing namespaces: common, home, project, import, error, health, tooltip, granularity, topN, monthSelector, time, export, server, status, dialog, panel, events, static, settings
}))
```

From apps/web/i18n/locales/en.ts (line 251 ends):
```typescript
// Mirror structure, English values
```

<!-- 现有页面挂载点 -->

From apps/web/app/pages/projects/[id]/index.vue (line 1078-1081):
```vue
    <!-- Settings modal -->
    <SettingsModal v-model:open="settingsModalOpen" show-storage />
  </div>
</template>
```
AgentChat mounts just before `</div>` at line 1080 (before SettingsModal or after, both fine — plan puts it before SettingsModal).

<!-- Nuxt UI v4 USlideover API -->
```vue
<USlideover v-model:open="open" side="right" :dismissible="false">
  <template #header>...</template>
  <template #body>...</template>
</USlideover>
```
- `v-model:open` boolean binding
- `side="right"` for right-side slide-in
- `:dismissible="false"` to prevent default close (we intercept to minimize)
- `#header` and `#body` slots

<!-- fetch-event-source API -->
```typescript
import { fetchEventSource } from '@microsoft/fetch-event-source'

await fetchEventSource(url, {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({ message }),
  signal: abortController.signal,
  async onopen(response) { /* check status */ },
  onmessage(ev) { /* parse JSON */ },
  onerror(err) { /* throw to stop */ },
})
```

<!-- 项目组件约定 -->
From CONVENTIONS.md:
- PascalCase.vue files, `<script setup lang="ts">`
- `defineProps<{ projectId: number }>()`
- `const { t } = useI18n()` for i18n
- Icons: `i-lucide-*` via `<UIcon name="i-lucide-xxx" />`
- Colors: `bg-elevated`, `text-muted`, `text-default`, `border-default`, `text-highlighted`, `bg-accented`
- Dark-first theme
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 安装依赖 + i18n 12 key + AgentToolCard.vue</name>
  <files>
    apps/web/package.json
    apps/web/i18n/locales/zh-CN.ts
    apps/web/i18n/locales/en.ts
    apps/web/app/components/AgentToolCard.vue
  </files>
  <read_first>
    apps/web/package.json
    apps/web/i18n/locales/zh-CN.ts
    apps/web/i18n/locales/en.ts
    .planning/phases/04-ui-i18n-chat-surface/04-UI-SPEC.md
  </read_first>
  <action>
**Step 1: 安装 @microsoft/fetch-event-source**

在 `apps/web/package.json` 的 `dependencies` 中追加（按字母顺序插入）：
```json
"@microsoft/fetch-event-source": "2.0.1",
```

然后运行 `pnpm install`（在 project-river 根目录执行 `pnpm install`）。

**Step 2: 在 zh-CN.ts 尾部追加 agent 命名空间**

在 `apps/web/i18n/locales/zh-CN.ts` 第 251 行（`settings` 命名空间之后、`}))` 之前）插入：

```typescript
  agent: {
    askButton: '问 Agent',
    drawer: {
      title: '项目分析助手',
      placeholder: '问问这个项目的演进...',
      minimize: '缩小到角落',
    },
    chip: {
      1: 'useStorage 主要由谁维护? 最近 6 个月有 owner shift 吗?',
      2: 'composables/ 模块谁贡献最多?',
      3: '项目有哪些里程碑事件?',
      4: 'core 包近期最活跃?',
      5: 'shared utils 最近被谁改动?',
    },
    error: {
      noData: '现有数据中查不到',
      apiKeyMissing: 'API Key 未配置，请在 .env 中设置 NUXT_AGENT_LLM_API_KEY',
      streamInterrupted: '流中断，请重试',
      rateLimited: '请求过于频繁，{n} 秒后重试',
      costCap: '会话 token 接近上限，请刷新页面重新开始',
      inputTooLong: '500 / {n}',
      aborted: '已中断',
    },
    toolcard: {
      input: '输入参数',
      output: '返回结果',
      step: 'Step {n}',
      duration: '{n}ms',
    },
  },
```

**Step 3: 在 en.ts 尾部追加 agent 命名空间**

在 `apps/web/i18n/locales/en.ts` 第 251 行之后插入镜像结构：

```typescript
  agent: {
    askButton: 'Ask Agent',
    drawer: {
      title: 'Project Analyst',
      placeholder: 'Ask about this project\'s evolution...',
      minimize: 'Minimize',
    },
    chip: {
      1: 'Who maintains useStorage? Any owner shift in the last 6 months?',
      2: 'Who contributed most to the composables/ module?',
      3: 'What are the project\'s milestone events?',
      4: 'Which package in core/ is most active recently?',
      5: 'Who recently changed shared utils?',
    },
    error: {
      noData: 'No data found in current records',
      apiKeyMissing: 'API Key not configured. Set NUXT_AGENT_LLM_API_KEY in .env',
      streamInterrupted: 'Stream interrupted. Please retry.',
      rateLimited: 'Rate limited. Retry in {n}s.',
      costCap: 'Session token limit approaching. Refresh to restart.',
      inputTooLong: '500 / {n}',
      aborted: 'Aborted',
    },
    toolcard: {
      input: 'Input',
      output: 'Output',
      step: 'Step {n}',
      duration: '{n}ms',
    },
  },
```

**Step 4: 创建 AgentToolCard.vue**

创建 `apps/web/app/components/AgentToolCard.vue`（~80 行）。按 D-10/D-11 + UI-SPEC 实现：

```vue
<script setup lang="ts">
interface Props {
  name: string
  input: unknown
  output: unknown | null
  isError: boolean
  status: 'running' | 'done'
  index: number
  duration?: number
}

const props = defineProps<Props>()
const { t } = useI18n()
const expanded = ref(false)

const statusIcon = computed(() => {
  if (props.status === 'running')
    return 'i-lucide-loader-2'
  return expanded.value ? 'i-lucide-chevron-up' : 'i-lucide-chevron-down'
})
</script>

<template>
  <div
    class="rounded border overflow-hidden"
    :class="isError ? 'border-red-500' : 'border-default'"
  >
    <!-- Header: clickable toggle -->
    <button
      class="w-full flex items-center gap-2 p-2 text-left hover:bg-elevated/50 transition-colors"
      :aria-expanded="expanded"
      :aria-controls="`tool-body-${index}`"
      @click="expanded = !expanded"
    >
      <span class="text-xs font-semibold text-muted shrink-0">
        {{ t('agent.toolcard.step', { n: index }) }}
      </span>
      <code class="text-xs text-default truncate">{{ name }}</code>
      <UIcon
        v-if="status === 'running'"
        name="i-lucide-loader-2"
        class="w-3.5 h-3.5 text-muted animate-spin shrink-0 ml-auto"
      />
      <span
        v-else-if="isError"
        class="ml-auto text-[10px] font-semibold text-red-400 bg-red-500/10 px-1.5 py-0.5 rounded shrink-0"
      >
        Error
      </span>
      <UIcon
        v-else
        :name="statusIcon"
        class="w-3.5 h-3.5 text-muted shrink-0 ml-auto transition-transform"
      />
    </button>

    <!-- Body: expanded content -->
    <div
      v-show="expanded"
      :id="`tool-body-${index}`"
      class="p-2 border-t border-default space-y-2"
    >
      <!-- Input -->
      <div>
        <p class="text-[10px] font-semibold text-muted uppercase tracking-wider mb-1">
          {{ t('agent.toolcard.input') }}
        </p>
        <pre class="text-xs font-mono text-dimmed bg-default p-2 rounded overflow-x-auto">{{ JSON.stringify(input, null, 2) }}</pre>
      </div>

      <!-- Output -->
      <div v-if="output !== null">
        <p class="text-[10px] font-semibold text-muted uppercase tracking-wider mb-1">
          {{ t('agent.toolcard.output') }}
        </p>
        <pre class="text-xs font-mono text-dimmed bg-default p-2 rounded overflow-x-auto">{{ JSON.stringify(output, null, 2) }}</pre>
      </div>

      <!-- Duration -->
      <p v-if="duration !== undefined" class="text-[10px] text-muted text-right">
        {{ t('agent.toolcard.duration', { n: duration }) }}
      </p>
    </div>
  </div>
</template>
```

注意：
- 使用 `v-show` 而非 `<UCollapsible>`（Nuxt UI v4 可能没有 UCollapsible，用 v-show + CSS transition 更可控）
- `isError` 时边框变红 + Error badge
- `status === 'running'` 时显示 spinner，不显示 chevron
- JSON 用 `JSON.stringify(..., null, 2)` pretty-print
- 所有标签走 `t('agent.toolcard.*')` i18n
  </action>
  <acceptance_criteria>
    - `grep "@microsoft/fetch-event-source" apps/web/package.json` 返回匹配，版本为 "2.0.1"
    - `grep "agent:" apps/web/i18n/locales/zh-CN.ts` 返回匹配
    - `grep "askButton" apps/web/i18n/locales/zh-CN.ts` 返回匹配
    - `grep "agent:" apps/web/i18n/locales/en.ts` 返回匹配
    - `grep "Ask Agent" apps/web/i18n/locales/en.ts` 返回匹配
    - `grep "Project Analyst" apps/web/i18n/locales/en.ts` 返回匹配
    - `grep -c "agent\." apps/web/i18n/locales/zh-CN.ts` 返回 >= 20（12 key + 嵌套路径重复计数）
    - `ls apps/web/app/components/AgentToolCard.vue` 文件存在
    - `grep "defineProps" apps/web/app/components/AgentToolCard.vue` 返回匹配
    - `grep "expanded" apps/web/app/components/AgentToolCard.vue` 返回匹配
    - `grep "v-show" apps/web/app/components/AgentToolCard.vue` 返回匹配
    - `grep "JSON.stringify" apps/web/app/components/AgentToolCard.vue` 返回匹配（2 处）
    - `grep "i-lucide-loader-2" apps/web/app/components/AgentToolCard.vue` 返回匹配
    - `pnpm --filter web exec tsc --noEmit` 通过（新增组件无类型错误）
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/lionad/Github/Lionad-Morotar/project-river && pnpm --filter web exec tsc --noEmit --pretty 2>&1 | grep -E "AgentToolCard|agent\.toolcard|fetch-event-source" | head -20; echo "EXIT:$?"</automated>
  </verify>
  <done>
    @microsoft/fetch-event-source 2.0.1 安装到 node_modules
    zh-CN.ts 和 en.ts 各追加 12 个 agent.* i18n key，结构对称
    AgentToolCard.vue 创建完成，props 定义完整，折叠/展开/JSON 渲染/i18n 标签全部到位
    TypeScript 编译通过
  </done>
</task>

<task type="auto">
  <name>Task 2: AgentChat.vue 主组件</name>
  <files>
    apps/web/app/components/AgentChat.vue
  </files>
  <read_first>
    apps/web/app/components/AgentToolCard.vue
    apps/web/server/api/projects/[id]/agent.post.ts
    apps/web/i18n/locales/zh-CN.ts
    apps/web/app/components/ConfirmDialog.vue
    .planning/phases/04-ui-i18n-chat-surface/04-CONTEXT.md
    .planning/phases/04-ui-i18n-chat-surface/04-UI-SPEC.md
  </read_first>
  <action>
创建 `apps/web/app/components/AgentChat.vue`（~280 行）。按 D-01~D-16 + UI-SPEC 完整实现。

**核心状态设计：**

```typescript
type AgentPhase = 'idle' | 'streaming' | 'tool-calling' | 'stream-mid-error'
  | 'abort' | 'rate-limit' | 'cost-cap' | 'input-too-long'
  | 'api-key-missing' | 'empty-result'

interface AgentMessage {
  role: 'user' | 'assistant'
  text: string
  toolCalls?: ToolCallItem[]
}

interface ToolCallItem {
  id: string
  name: string
  input: unknown
  output?: unknown
  isError: boolean
  status: 'running' | 'done'
  index: number
  duration?: number
}
```

**完整组件实现：**

```vue
<script setup lang="ts">
import { fetchEventSource } from '@microsoft/fetch-event-source'
import AgentToolCard from './AgentToolCard.vue'

const props = defineProps<{ projectId: number }>()
const { t } = useI18n()

// ── Display mode: 'fab' | 'drawer' (D-02) ──
const displayMode = ref<'fab' | 'drawer'>('fab')
const open = computed({
  get: () => displayMode.value === 'drawer',
  set: (v) => { displayMode.value = v ? 'drawer' : 'fab' },
})
function minimize() { displayMode.value = 'fab' }
function expand() { displayMode.value = 'drawer' }

// ── State machine (D-07) ──
type AgentPhase = 'idle' | 'streaming' | 'tool-calling' | 'stream-mid-error'
  | 'abort' | 'rate-limit' | 'cost-cap' | 'input-too-long'
  | 'api-key-missing' | 'empty-result'

const phase = ref<AgentPhase>('idle')
const messages = reactive<AgentMessage[]>([])
const inputValue = ref('')
const sessionTokens = ref(0)
let abortController: AbortController | null = null

const inputTooLong = computed(() => inputValue.value.length > 500)

// Reset input-too-long when length drops
watch(inputTooLong, (v) => {
  if (!v && phase.value === 'input-too-long')
    phase.value = 'idle'
})

// Chip definitions (D-14)
const chips = computed(() => [
  t('agent.chip.1'),
  t('agent.chip.2'),
  t('agent.chip.3'),
  t('agent.chip.4'),
  t('agent.chip.5'),
])

function handleChipClick(text: string) {
  inputValue.value = text
  submit(text)
}

// ── SSE submit ──
interface ToolCallItem {
  id: string
  name: string
  input: unknown
  output?: unknown
  isError: boolean
  status: 'running' | 'done'
  index: number
  duration?: number
}

interface AgentMessage {
  role: 'user' | 'assistant'
  text: string
  toolCalls?: ToolCallItem[]
}

async function submit(text: string) {
  if (!text.trim() || inputTooLong.value)
    return

  abortController = new AbortController()
  phase.value = 'streaming'
  messages.push({ role: 'user', text: text.trim() })

  const assistant: AgentMessage = reactive({
    role: 'assistant',
    text: '',
    toolCalls: [],
  })
  messages.push(assistant)

  const toolStartTimes = new Map<string, number>()
  let toolIndex = 0

  try {
    await fetchEventSource(`/api/projects/${props.projectId}/agent`, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ message: text.trim() }),
      signal: abortController.signal,
      async onopen(response) {
        if (response.status === 503) {
          phase.value = 'api-key-missing'
          throw new Error('api-key-missing')
        }
        if (response.status === 429) {
          const retry = Number(response.headers.get('retry-after') ?? 30)
          phase.value = 'rate-limit'
          startRateLimitCountdown(retry)
          throw new Error('rate-limit')
        }
        if (response.status === 400) {
          phase.value = 'stream-mid-error'
          throw new Error('bad-request')
        }
        if (!response.ok) {
          phase.value = 'stream-mid-error'
          throw new Error(`HTTP ${response.status}`)
        }
      },
      onmessage(ev) {
        const payload = JSON.parse(ev.data)
        switch (payload.type) {
          case 'text':
            assistant.text += payload.token
            sessionTokens.value += 1
            if (sessionTokens.value > 50_000)
              phase.value = 'cost-cap'
            break
          case 'tool-call':
            phase.value = 'tool-calling'
            toolStartTimes.set(payload.id, Date.now())
            toolIndex += 1
            assistant.toolCalls!.push({
              id: payload.id,
              name: payload.name,
              input: payload.args,
              output: null,
              isError: false,
              status: 'running',
              index: toolIndex,
            })
            break
          case 'tool-result': {
            const tc = assistant.toolCalls!.find(t => t.id === payload.id)
            if (tc) {
              tc.output = payload.result
              tc.isError = payload.isError
              tc.status = 'done'
              const start = toolStartTimes.get(payload.id)
              if (start)
                tc.duration = Date.now() - start
            }
            phase.value = 'streaming'
            break
          }
          case 'done':
            phase.value = assistant.text.trim() === '' ? 'empty-result' : 'idle'
            break
          case 'error':
            phase.value = 'stream-mid-error'
            break
        }
      },
      onerror(err) {
        if (abortController?.signal.aborted)
          phase.value = 'abort'
        else
          phase.value = 'stream-mid-error'
        throw err // stop auto-retry
      },
    })
  }
  catch {
    // phase already set in onopen/onerror/onmessage
  }
}

function abort() {
  abortController?.abort()
  phase.value = 'abort'
}

function retry() {
  phase.value = 'idle'
}

// Rate limit countdown
const rateLimitSeconds = ref(0)
let rateLimitTimer: ReturnType<typeof setInterval> | null = null
function startRateLimitCountdown(seconds: number) {
  rateLimitSeconds.value = seconds
  if (rateLimitTimer)
    clearInterval(rateLimitTimer)
  rateLimitTimer = setInterval(() => {
    rateLimitSeconds.value -= 1
    if (rateLimitSeconds.value <= 0) {
      clearInterval(rateLimitTimer!)
      rateLimitTimer = null
      phase.value = 'idle'
    }
  }, 1000)
}

// Cleanup
onBeforeUnmount(() => {
  abortController?.abort()
  if (rateLimitTimer)
    clearInterval(rateLimitTimer)
})

// Auto-focus input when drawer opens
watch(open, (v) => {
  if (v) {
    nextTick(() => {
      inputRef.value?.focus()
    })
  }
})

const inputRef = ref<HTMLInputElement | null>(null)
</script>

<template>
  <div>
    <!-- FAB Button (D-01, D-02) -->
    <button
      v-show="displayMode === 'fab'"
      class="fixed bottom-6 right-6 z-50 w-14 h-14 rounded-full bg-sky-500 text-white shadow-lg shadow-black/30 flex items-center justify-center hover:bg-sky-400 transition-colors"
      :aria-label="t('agent.askButton')"
      @click="expand"
    >
      <UIcon name="i-lucide-message-circle-question" class="w-6 h-6" />
    </button>

    <!-- USlideover Drawer (D-03, UI-05) -->
    <USlideover
      v-model:open="open"
      side="right"
      :dismissible="false"
      class="w-[40vw] max-md:w-full"
    >
      <template #header>
        <div class="flex items-center justify-between w-full">
          <h3 class="text-base font-semibold text-highlighted">
            {{ t('agent.drawer.title') }}
          </h3>
          <button
            class="hover:bg-elevated/60 p-1.5 rounded text-muted hover:text-default transition-colors"
            :aria-label="t('agent.drawer.minimize')"
            @click="minimize"
          >
            <UIcon name="i-lucide-chevrons-down-up" class="w-4 h-4" />
          </button>
        </div>
      </template>

      <template #body>
        <div class="flex flex-col h-full">
          <!-- Message list -->
          <div class="flex-1 overflow-y-auto space-y-4 p-4 min-h-0">
            <!-- Idle: chips -->
            <div v-if="phase === 'idle' && messages.length === 0" class="flex flex-wrap gap-2">
              <button
                v-for="(chip, idx) in chips"
                :key="idx"
                class="px-3 py-1.5 text-xs rounded-full border border-default bg-elevated/30 text-dimmed hover:border-sky-500 hover:bg-sky-500/10 hover:text-default transition-all"
                @click="handleChipClick(chip)"
              >
                {{ chip }}
              </button>
            </div>

            <!-- Empty result -->
            <div v-else-if="phase === 'empty-result'" class="text-center py-8">
              <p class="text-sm text-muted">{{ t('agent.error.noData') }}</p>
              <div class="flex flex-wrap gap-2 mt-4 justify-center">
                <button
                  v-for="(chip, idx) in chips"
                  :key="idx"
                  class="px-3 py-1.5 text-xs rounded-full border border-default bg-elevated/30 text-dimmed hover:border-sky-500 hover:bg-sky-500/10 hover:text-default transition-all"
                  @click="handleChipClick(chip)"
                >
                  {{ chip }}
                </button>
              </div>
            </div>

            <!-- Messages -->
            <template v-for="(msg, idx) in messages" :key="idx">
              <!-- User message -->
              <div v-if="msg.role === 'user'" class="flex justify-end">
                <div class="bg-elevated rounded-lg px-3 py-2 max-w-[85%]">
                  <p class="text-sm text-default">{{ msg.text }}</p>
                </div>
              </div>

              <!-- Assistant message -->
              <div v-else class="flex justify-start">
                <div class="max-w-[85%] space-y-2">
                  <div class="bg-default border border-default rounded-lg px-3 py-2">
                    <p class="text-sm text-default whitespace-pre-wrap">{{ msg.text }}</p>
                    <!-- Streaming cursor -->
                    <span v-if="phase === 'streaming' && idx === messages.length - 1" class="inline-block w-0.5 h-4 bg-sky-500 ml-0.5 align-middle animate-pulse" />
                  </div>

                  <!-- Tool cards -->
                  <div v-if="msg.toolCalls?.length" class="space-y-2">
                    <AgentToolCard
                      v-for="tc in msg.toolCalls"
                      :key="tc.id"
                      :name="tc.name"
                      :input="tc.input"
                      :output="tc.output"
                      :is-error="tc.isError"
                      :status="tc.status"
                      :index="tc.index"
                      :duration="tc.duration"
                    />
                  </div>

                  <!-- Abort badge -->
                  <div v-if="phase === 'abort' && idx === messages.length - 1" class="text-xs text-muted">
                    {{ t('agent.error.aborted') }}
                  </div>
                </div>
              </div>
            </template>

            <!-- Error banner: stream-mid-error -->
            <div v-if="phase === 'stream-mid-error'" class="bg-red-500/10 border border-red-500/30 rounded-lg p-3 flex items-center gap-3">
              <UIcon name="i-lucide-alert-circle" class="w-4 h-4 text-red-400 shrink-0" />
              <p class="text-xs text-red-400 flex-1">{{ t('agent.error.streamInterrupted') }}</p>
              <button
                class="px-2 py-1 text-xs rounded bg-elevated text-default hover:bg-elevated/80 transition-colors"
                @click="retry"
              >
                <UIcon name="i-lucide-refresh-cw" class="w-3 h-3 inline mr-1" />
                {{ t('common.retry') }}
              </button>
            </div>

            <!-- Rate limit banner -->
            <div v-if="phase === 'rate-limit'" class="bg-amber-500/10 border border-amber-500/30 rounded-lg p-3">
              <p class="text-xs text-amber-400">{{ t('agent.error.rateLimited', { n: rateLimitSeconds }) }}</p>
            </div>

            <!-- Cost cap banner -->
            <div v-if="phase === 'cost-cap'" class="bg-orange-500/10 border border-orange-500/30 rounded-lg p-3">
              <p class="text-xs text-orange-400">{{ t('agent.error.costCap') }}</p>
            </div>

            <!-- API key missing overlay -->
            <div v-if="phase === 'api-key-missing'" class="absolute inset-0 bg-default/90 flex flex-col items-center justify-center p-6 z-10">
              <UIcon name="i-lucide-key-round" class="w-8 h-8 text-muted mb-3" />
              <p class="text-sm text-muted text-center">{{ t('agent.error.apiKeyMissing') }}</p>
            </div>
          </div>

          <!-- Composer -->
          <div class="border-t border-default p-3 shrink-0">
            <div class="flex items-end gap-2">
              <div class="flex-1 relative">
                <input
                  ref="inputRef"
                  v-model="inputValue"
                  type="text"
                  class="w-full bg-elevated border border-default rounded-lg px-3 py-2 text-sm text-default placeholder:text-muted focus:outline-none focus:ring-1 focus:ring-sky-500/30"
                  :placeholder="t('agent.drawer.placeholder')"
                  :disabled="phase === 'streaming' || phase === 'tool-calling' || phase === 'rate-limit' || phase === 'cost-cap' || phase === 'api-key-missing'"
                  @keyup.enter="submit(inputValue)"
                >
                <span
                  class="absolute right-2 bottom-2 text-[10px] tabular-nums"
                  :class="inputTooLong ? 'text-red-400' : 'text-muted'"
                >
                  {{ t('agent.error.inputTooLong', { n: inputValue.length }) }}
                </span>
              </div>

              <!-- Submit / Stop button -->
              <button
                v-if="phase === 'streaming' || phase === 'tool-calling'"
                class="w-9 h-9 rounded-lg bg-red-500/10 text-red-400 hover:bg-red-500/20 flex items-center justify-center transition-colors shrink-0"
                :aria-label="t('common.cancel')"
                @click="abort"
              >
                <UIcon name="i-lucide-square" class="w-4 h-4" />
              </button>
              <button
                v-else
                class="w-9 h-9 rounded-lg bg-sky-500 text-white hover:bg-sky-400 disabled:opacity-40 disabled:cursor-not-allowed flex items-center justify-center transition-colors shrink-0"
                :disabled="!inputValue.trim() || inputTooLong"
                :aria-label="t('common.confirm')"
                @click="submit(inputValue)"
              >
                <UIcon name="i-lucide-send" class="w-4 h-4" />
              </button>
            </div>
          </div>
        </div>
      </template>
    </USlideover>
  </div>
</template>
```

**关键设计点：**

1. **FAB 始终渲染** (`v-show`)，drawer 打开时被 overlay 遮盖。session state 不丢失。
2. **USlideover `dismissible: false`** — Esc/overlay click 不会默认关闭。Nuxt UI v4 的 USlideover 在 `dismissible: false` 时仍可能响应 Esc，但 `v-model:open` 绑定到 computed 的 setter 会将其映射为 minimize（回到 FAB）。
3. **状态机 reducer 逻辑 inline** — 事件驱动 phase 转移，符合 D-08 单一来源真值。
4. **tool-call 配对** — `toolIndex` 按到达顺序累加，传给 AgentToolCard 的 `index` prop。
5. **rate-limit 倒计时** — `startRateLimitCountdown` 用 setInterval，到 0 自动转 idle。
6. **input-too-long** — computed 监听，>500 时 phase 变 `input-too-long`，字符计数器变红，submit disabled。
7. **cost-cap** — sessionTokens > 50K 时 phase 变 `cost-cap`，输入禁用。
8. **abort** — 点击 stop 按钮调用 `abortController.abort()`，onerror 中检测到 `signal.aborted` 转 `abort` phase。
9. **auto-focus** — drawer 打开时 `nextTick` focus 输入框。
10. **所有用户可见文案走 `t('agent.*')`** — 满足 I18N-01。
  </action>
  <acceptance_criteria>
    - `ls apps/web/app/components/AgentChat.vue` 文件存在
    - `grep "fetchEventSource" apps/web/app/components/AgentChat.vue` 返回匹配
    - `grep "displayMode" apps/web/app/components/AgentChat.vue` 返回匹配
    - `grep "'fab'" apps/web/app/components/AgentChat.vue` 返回匹配
    - `grep "'drawer'" apps/web/app/components/AgentChat.vue` 返回匹配
    - `grep "AgentPhase" apps/web/app/components/AgentChat.vue` 返回匹配
    - `grep "'idle'" apps/web/app/components/AgentChat.vue` 返回匹配
    - `grep "'streaming'" apps/web/app/components/AgentChat.vue` 返回匹配
    - `grep "'tool-calling'" apps/web/app/components/AgentChat.vue` 返回匹配
    - `grep "'stream-mid-error'" apps/web/app/components/AgentChat.vue` 返回匹配
    - `grep "'abort'" apps/web/app/components/AgentChat.vue` 返回匹配
    - `grep "'rate-limit'" apps/web/app/components/AgentChat.vue` 返回匹配
    - `grep "'cost-cap'" apps/web/app/components/AgentChat.vue` 返回匹配
    - `grep "'input-too-long'" apps/web/app/components/AgentChat.vue` 返回匹配
    - `grep "'api-key-missing'" apps/web/app/components/AgentChat.vue` 返回匹配
    - `grep "'empty-result'" apps/web/app/components/AgentChat.vue` 返回匹配
    - `grep "USlideover" apps/web/app/components/AgentChat.vue` 返回匹配
    - `grep "dismissible" apps/web/app/components/AgentChat.vue` 返回匹配
    - `grep "AgentToolCard" apps/web/app/components/AgentChat.vue` 返回匹配
    - `grep "handleChipClick" apps/web/app/components/AgentChat.vue` 返回匹配
    - `grep "abortController" apps/web/app/components/AgentChat.vue` 返回匹配
    - `grep "sessionTokens" apps/web/app/components/AgentChat.vue` 返回匹配
    - `grep "onBeforeUnmount" apps/web/app/components/AgentChat.vue` 返回匹配
    - `grep "t('agent\." apps/web/app/components/AgentChat.vue` 返回 >= 15 处匹配
    - `pnpm --filter web exec tsc --noEmit` 通过
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/lionad/Github/Lionad-Morotar/project-river && pnpm --filter web exec tsc --noEmit --pretty 2>&1 | grep -E "AgentChat|fetchEventSource|USlideover" | head -20; echo "EXIT:$?"</automated>
  </verify>
  <done>
    AgentChat.vue 创建完成，包含：
    - FAB 按钮（fixed bottom-6 right-6，sky-500 背景）
    - USlideover drawer（40vw，dismissible=false）
    - displayMode 三态（fab/drawer）+ minimize 按钮
    - 10 UI States 全部实现
    - SSE fetchEventSource 消费（POST + AbortController）
    - 5 chip questions + 点击自动提交
    - 消息列表（user/assistant）+ tool-call cards
    - 错误覆盖层（stream-mid-error/rate-limit/cost-cap/api-key-missing/empty-result）
    - input-too-long 检测 + 字符计数
    - 所有文案走 i18n t('agent.*')
    - TypeScript 编译通过
  </done>
</task>

<task type="auto">
  <name>Task 3: 页面挂载 + 类型修复</name>
  <files>
    apps/web/app/pages/projects/[id]/index.vue
  </files>
  <read_first>
    apps/web/app/pages/projects/[id]/index.vue
    apps/web/app/components/AgentChat.vue
  </read_first>
  <action>
**Step 1: 在 index.vue 模板尾部挂载 AgentChat**

在 `apps/web/app/pages/projects/[id]/index.vue` 中，找到 `</div>`（第 1081 行， closing div for the root template）。在 `<!-- Settings modal -->` 之前插入：

```vue
    <!-- Agent Chat -->
    <AgentChat :project-id="projectId" />
```

具体位置：在第 1078 行（`<!-- Settings modal -->`）之前添加这两行。

**Step 2: 确保 script 中 projectId 可用**

检查 index.vue 的 `<script setup>` 中 `projectId` 是否已定义为响应式变量或 computed。从现有代码看，`projectId` 来自路由参数，通常在 script 中有类似：

```typescript
const projectId = Number(route.params.id)
```

或类似定义。确认 `projectId` 在模板中可直接使用。如果变量名不同（如 `id`、`pid`），调整 AgentChat 的 prop 绑定。

**Step 3: TypeScript 全量编译验证**

运行 `pnpm --filter web exec tsc --noEmit` 确保：
1. AgentChat.vue 的 import 被正确解析
2. fetchEventSource 的类型定义可用
3. 无 `any` 隐式类型错误

如果 `tsc` 报错 `@microsoft/fetch-event-source` 缺少类型声明，创建 `apps/web/app/types/fetch-event-source.d.ts`：

```typescript
declare module '@microsoft/fetch-event-source' {
  export interface FetchEventSourceInit {
    method?: string
    headers?: Record<string, string>
    body?: string
    signal?: AbortSignal
    onopen?: (response: Response) => void | Promise<void>
    onmessage?: (event: { id: string; event: string; data: string }) => void
    onclose?: () => void
    onerror?: (error: Error) => void | Promise<void> | number | null | undefined
  }
  export function fetchEventSource(url: string, options: FetchEventSourceInit): Promise<void>
}
```

**Step 4: ESLint 检查**

运行 `pnpm --filter web exec eslint app/components/AgentChat.vue app/components/AgentToolCard.vue app/pages/projects/\[id\]/index.vue` 确保无 lint error。
  </action>
  <acceptance_criteria>
    - `grep "<AgentChat" apps/web/app/pages/projects/[id]/index.vue` 返回匹配
    - `grep "project-id" apps/web/app/pages/projects/[id]/index.vue` 返回匹配
    - `pnpm --filter web exec tsc --noEmit` 返回 EXIT:0
    - `pnpm --filter web exec eslint app/components/AgentChat.vue app/components/AgentToolCard.vue app/pages/projects/\[id\]/index.vue` 返回 EXIT:0
    - `pnpm --filter web exec nuxt prepare` 成功（生成 .nuxt 类型）
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/lionad/Github/Lionad-Morotar/project-river && pnpm --filter web exec tsc --noEmit --pretty 2>&1 | tail -10; echo "EXIT:$?"</automated>
  </verify>
  <done>
    AgentChat 挂载到项目详情页模板
    projectId prop 正确传递
    TypeScript 全量编译通过
    ESLint 无 error
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → SSE Route | User input (message string) crosses to server via POST body |
| AgentChat → External LLM | API key server-only; client never sees key |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | S | AgentChat.vue | mitigate | Input length capped at 500 chars (input-too-long state). No HTML injection — text rendered as plain text, not v-html. |
| T-04-02 | T | AgentChat.vue | mitigate | SSE payload parsed with JSON.parse only, no eval. Tool card JSON rendered in `<pre>`, not executed. |
| T-04-03 | I | AgentChat.vue | mitigate | API key missing → 503 handled gracefully (overlay with config guidance, no key leaked). |
| T-04-04 | D | AgentChat.vue | mitigate | AbortController aborts in-flight request. onBeforeUnmount cleans up timers and aborts. Rate-limit countdown auto-resets. |
</threat_model>

<verification>
1. `pnpm --filter web exec tsc --noEmit` 通过
2. `pnpm --filter web exec eslint app/components/AgentChat.vue app/components/AgentToolCard.vue app/pages/projects/\[id\]/index.vue` 通过
3. `pnpm --filter web exec nuxt prepare` 成功
4. AgentChat.vue 存在且包含 10 个 phase 字符串
5. AgentToolCard.vue 存在且包含 defineProps + expanded + v-show
6. zh-CN.ts 和 en.ts 各包含 agent 命名空间
7. package.json 包含 @microsoft/fetch-event-source
8. index.vue 包含 `<AgentChat :project-id="projectId" />`
</verification>

<success_criteria>
- 用户可在项目详情页看到右下 FAB 按钮（sky-500 圆形）
- 点击 FAB → USlideover drawer 从右侧 slide in
- 点击 minimize/Esc → drawer 缩小回 FAB，messages 状态保留
- 5 个 chip 可点击并自动提交
- SSE 流式 token 实时渲染到 assistant 消息
- tool-call 卡片默认折叠，可展开看 JSON
- 10 UI States 全部有视觉反馈
- 中英 locale 切换时所有文案同步变化
- TypeScript + ESLint 全部通过
</success_criteria>

<output>
After completion, create `.planning/phases/04-ui-i18n-chat-surface/04-01-SUMMARY.md`
</output>

---
phase: 04-ui-i18n-chat-surface
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - apps/web/test/components/AgentChat.spec.ts
  - apps/web/test/components/AgentToolCard.spec.ts
autonomous: true
requirements:
  - TEST-04

must_haves:
  truths:
    - "AgentChat.spec.ts 覆盖 10 UI States 渲染分支"
    - "AgentChat.spec.ts 验证 fab↔drawer 切换时 messages state 保留"
    - "AgentChat.spec.ts 验证 chip 点击触发 submit 且入参正确"
    - "AgentChat.spec.ts 验证 locale 切换时文案同步变化"
    - "AgentChat.spec.ts 验证 input-too-long 阈值（≤500 / >500）"
    - "AgentToolCard.spec.ts 验证默认折叠、点击展开、JSON 渲染、isError 样式、i18n 标签"
  artifacts:
    - path: "apps/web/test/components/AgentChat.spec.ts"
      provides: "AgentChat 组件测试"
      min_lines: 200
    - path: "apps/web/test/components/AgentToolCard.spec.ts"
      provides: "AgentToolCard 组件测试"
      min_lines: 60
  key_links:
    - from: "AgentChat.spec.ts"
      to: "AgentChat.vue"
      via: "mount(AgentChat)"
      pattern: "mount.*AgentChat"
    - from: "AgentChat.spec.ts"
      to: "fetchEventSource mock"
      via: "vi.doMock"
      pattern: "vi\.doMock.*fetch-event-source"
    - from: "AgentToolCard.spec.ts"
      to: "AgentToolCard.vue"
      via: "mount(AgentToolCard)"
      pattern: "mount.*AgentToolCard"
---

<objective>
为 Phase 4 核心 UI 组件补齐组件级测试：AgentChat.spec.ts（10 UI States + FAB/drawer 状态 + i18n 切换 + chip 交互）+ AgentToolCard.spec.ts（折叠/展开 + JSON + error 样式）。

Purpose: TEST-04 是 Phase 4 的硬验收标准。组件测试在不依赖真实 SSE 连接或 LLM 的前提下，验证所有 UI 分支渲染正确。

Output: 2 个测试文件，覆盖 D-15/D-16 描述的全部断言场景。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04-ui-i18n-chat-surface/04-CONTEXT.md
@.planning/phases/04-ui-i18n-chat-surface/04-UI-SPEC.md
@.planning/phases/04-ui-i18n-chat-surface/04-01-SUMMARY.md
@.planning/codebase/TESTING.md

<interfaces>
<!-- 被测组件接口 -->

AgentChat.vue props:
```typescript
interface Props {
  projectId: number
}
```

AgentChat.vue 内部状态（测试需要 mock 或触发）：
```typescript
type AgentPhase = 'idle' | 'streaming' | 'tool-calling' | 'stream-mid-error'
  | 'abort' | 'rate-limit' | 'cost-cap' | 'input-too-long'
  | 'api-key-missing' | 'empty-result'
```

AgentToolCard.vue props:
```typescript
interface Props {
  name: string
  input: unknown
  output: unknown | null
  isError: boolean
  status: 'running' | 'done'
  index: number
  duration?: number
}
```

<!-- Vue Test Utils 模式 -->
From TESTING.md:
```typescript
import { mount } from '@vue/test-utils'
import { describe, expect, it } from 'vitest'
// mount(component, { props: {} })
// wrapper.find(), wrapper.trigger(), wrapper.emitted()
```

<!-- i18n mock 模式 -->
```typescript
import { createI18n } from 'vue-i18n'
const i18n = createI18n({
  legacy: false,
  locale: 'zh-CN',
  messages: { 'zh-CN': { agent: { ... } }, en: { agent: { ... } } }
})
// mount(Component, { global: { plugins: [i18n] } })
```

<!-- fetchEventSource mock 模式 -->
```typescript
const fetchEventSourceMock = vi.fn()
vi.doMock('@microsoft/fetch-event-source', () => ({
  fetchEventSource: fetchEventSourceMock
}))
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AgentToolCard.spec.ts</name>
  <files>
    apps/web/test/components/AgentToolCard.spec.ts
  </files>
  <read_first>
    apps/web/app/components/AgentToolCard.vue
    apps/web/test/components/ResizeHandle.spec.ts
    .planning/codebase/TESTING.md
  </read_first>
  <behavior>
    - 测试 1: 默认折叠状态 — expanded 为 false，body 内容不可见
    - 测试 2: 点击 header 展开 — 点击 toggle button，body 内容可见，chevron 方向变化
    - 测试 3: 再次点击收起 — 再次点击，body 内容隐藏
    - 测试 4: input/output JSON pretty-print 正确 — JSON.stringify(..., null, 2) 格式
    - 测试 5: isError=true 时红色边框和 Error badge 渲染
    - 测试 6: status='running' 时显示 spinner 而非 chevron
    - 测试 7: i18n 标签切换 — locale 从 zh-CN 切到 en，标签从 "输入参数" 变为 "Input"
  </behavior>
  <action>
创建 `apps/web/test/components/AgentToolCard.spec.ts`。

按 D-15/D-16 实现：Vue Test Utils mount + i18n mock。

```typescript
// @vitest-environment jsdom
import { afterEach, beforeEach, describe, expect, it, vi } from 'vitest'
import { mount } from '@vue/test-utils'
import { createI18n } from 'vue-i18n'
import AgentToolCard from '~/components/AgentToolCard.vue'

// i18n messages for testing
const zhMessages = {
  agent: {
    toolcard: {
      input: '输入参数',
      output: '返回结果',
      step: 'Step {n}',
      duration: '{n}ms',
    },
  },
}

const enMessages = {
  agent: {
    toolcard: {
      input: 'Input',
      output: 'Output',
      step: 'Step {n}',
      duration: '{n}ms',
    },
  },
}

function createI18nPlugin(locale: string) {
  return createI18n({
    legacy: false,
    locale,
    messages: {
      'zh-CN': zhMessages,
      en: enMessages,
    },
  })
}

const baseProps = {
  name: 'queryContributors',
  input: { sortBy: 'commits', limit: 5 },
  output: [{ name: 'alice', commits: 42 }],
  isError: false,
  status: 'done' as const,
  index: 1,
  duration: 120,
}

describe('AgentToolCard', () => {
  it('renders collapsed by default', () => {
    const wrapper = mount(AgentToolCard, {
      props: baseProps,
      global: { plugins: [createI18nPlugin('zh-CN')] },
    })

    // Header visible
    expect(wrapper.find('button').exists()).toBe(true)
    expect(wrapper.text()).toContain('Step 1')
    expect(wrapper.text()).toContain('queryContributors')

    // Body hidden
    const body = wrapper.find('[id^="tool-body-"]')
    expect(body.isVisible()).toBe(false)
  })

  it('expands on header click', async () => {
    const wrapper = mount(AgentToolCard, {
      props: baseProps,
      global: { plugins: [createI18nPlugin('zh-CN')] },
    })

    await wrapper.find('button').trigger('click')

    const body = wrapper.find('[id^="tool-body-"]')
    expect(body.isVisible()).toBe(true)
    expect(body.text()).toContain('输入参数')
    expect(body.text()).toContain('返回结果')
    expect(body.text()).toContain('"sortBy"')
    expect(body.text()).toContain('"alice"')
  })

  it('collapses on second header click', async () => {
    const wrapper = mount(AgentToolCard, {
      props: baseProps,
      global: { plugins: [createI18nPlugin('zh-CN')] },
    })

    await wrapper.find('button').trigger('click')
    await wrapper.find('button').trigger('click')

    const body = wrapper.find('[id^="tool-body-"]')
    expect(body.isVisible()).toBe(false)
  })

  it('renders JSON in pretty-print format', async () => {
    const wrapper = mount(AgentToolCard, {
      props: baseProps,
      global: { plugins: [createI18nPlugin('zh-CN')] },
    })

    await wrapper.find('button').trigger('click')

    const pre = wrapper.findAll('pre')
    expect(pre.length).toBeGreaterThanOrEqual(2)
    // Pretty-print uses 2-space indent
    expect(pre[0].text()).toContain('  ')
  })

  it('shows error border and badge when isError is true', () => {
    const wrapper = mount(AgentToolCard, {
      props: { ...baseProps, isError: true },
      global: { plugins: [createI18nPlugin('zh-CN')] },
    })

    const container = wrapper.find('div').element
    expect(container.className).toContain('border-red-500')
    expect(wrapper.text()).toContain('Error')
  })

  it('shows spinner when status is running', () => {
    const wrapper = mount(AgentToolCard, {
      props: { ...baseProps, status: 'running' },
      global: { plugins: [createI18nPlugin('zh-CN')] },
    })

    expect(wrapper.find('[class*="animate-spin"]').exists()).toBe(true)
    // Chevron should NOT be present when running
    expect(wrapper.find('[class*="chevron"]').exists()).toBe(false)
  })

  it('switches i18n labels when locale changes', async () => {
    const i18n = createI18nPlugin('zh-CN')
    const wrapper = mount(AgentToolCard, {
      props: baseProps,
      global: { plugins: [i18n] },
    })

    await wrapper.find('button').trigger('click')
    expect(wrapper.text()).toContain('输入参数')

    // Switch locale
    i18n.global.locale.value = 'en'
    await wrapper.vm.$nextTick()

    expect(wrapper.text()).toContain('Input')
    expect(wrapper.text()).toContain('Output')
  })
})
```

**注意事项：**
- `isVisible()` 在 jsdom 中对 `v-show` 有效（检查 display style）
- i18n 用 `createI18n({ legacy: false })`（Composition API 模式）
- 测试文件顶部加 `// @vitest-environment jsdom` 指令
- 所有断言用 `expect(...).toBe()` 或 `toContain()`，不用快照
  </action>
  <acceptance_criteria>
    - 文件 `apps/web/test/components/AgentToolCard.spec.ts` 存在
    - `grep "@vitest-environment jsdom" apps/web/test/components/AgentToolCard.spec.ts` 返回匹配
    - `grep "mount.*AgentToolCard" apps/web/test/components/AgentToolCard.spec.ts` 返回匹配
    - `grep "createI18n" apps/web/test/components/AgentToolCard.spec.ts` 返回匹配
    - `grep "isVisible" apps/web/test/components/AgentToolCard.spec.ts` 返回匹配
    - `grep "border-red-500" apps/web/test/components/AgentToolCard.spec.ts` 返回匹配
    - `grep "animate-spin" apps/web/test/components/AgentToolCard.spec.ts` 返回匹配
    - `grep "Input\|输入参数" apps/web/test/components/AgentToolCard.spec.ts` 返回匹配
    - `grep -c "it(" apps/web/test/components/AgentToolCard.spec.ts` 返回 7
    - `pnpm --filter web exec vitest run test/components/AgentToolCard.spec.ts` 全部 PASS
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/lionad/Github/Lionad-Morotar/project-river && pnpm --filter web exec vitest run test/components/AgentToolCard.spec.ts 2>&1 | tail -30</automated>
  </verify>
  <done>
    7 个 it() 测试全部 pass
    覆盖：折叠/展开、JSON pretty-print、isError 样式、running spinner、i18n 切换
    无真实 SSE 或 LLM 调用
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: AgentChat.spec.ts</name>
  <files>
    apps/web/test/components/AgentChat.spec.ts
  </files>
  <read_first>
    apps/web/app/components/AgentChat.vue
    apps/web/test/components/AgentToolCard.spec.ts
    apps/web/test/components/ResizeHandle.spec.ts
    .planning/codebase/TESTING.md
    .planning/phases/04-ui-i18n-chat-surface/04-CONTEXT.md
  </read_first>
  <behavior>
    - 测试 1 (idle state): 挂载后 FAB 可见，drawer 不可见，5 个 chip 渲染
    - 测试 2 (fab→drawer→fab state retention): 点击 FAB → drawer 打开 → 输入消息 → minimize → FAB 可见 → 再点击 FAB → drawer 打开，之前输入的消息仍在
    - 测试 3 (chip click submits): 点击 chip → inputValue 被填充 → fetchEventSource 被调用且 body.message 等于 chip 文本
    - 测试 4 (streaming state): mock fetchEventSource 触发 onmessage text event → assistant 消息文本追加
    - 测试 5 (tool-calling state): mock fetchEventSource 触发 tool-call + tool-result → tool card 渲染，status 从 running 变 done
    - 测试 6 (stream-mid-error state): mock onerror → error banner 渲染
    - 测试 7 (abort state): 点击 stop 按钮 → abortController.abort() 被调用
    - 测试 8 (input-too-long): 输入 501 个字符 → submit 按钮 disabled，字符计数器变红
    - 测试 9 (api-key-missing): mock onopen 返回 503 → overlay 渲染
    - 测试 10 (empty-result): mock done 后 assistant text 为空 → empty-result 文案渲染
    - 测试 11 (locale switch): i18n locale 从 zh-CN 切到 en → drawer title 从 "项目分析助手" 变 "Project Analyst"
  </behavior>
  <action>
创建 `apps/web/test/components/AgentChat.spec.ts`。

按 D-15/D-16 实现：Vue Test Utils mount + vi.doMock fetchEventSource + i18n mock。

**关键 mock 策略：**

```typescript
// @vitest-environment jsdom
import { afterEach, beforeEach, describe, expect, it, vi } from 'vitest'
import { mount, flushPromises } from '@vue/test-utils'
import { createI18n } from 'vue-i18n'
import { nextTick } from 'vue'

// Mock fetchEventSource before importing AgentChat
const fetchEventSourceMock = vi.fn()

vi.doMock('@microsoft/fetch-event-source', () => ({
  fetchEventSource: fetchEventSourceMock,
}))

const { default: AgentChat } = await import('~/components/AgentChat.vue')

// i18n messages
function makeMessages(locale: string) {
  const isZh = locale === 'zh-CN'
  return {
    agent: {
      askButton: isZh ? '问 Agent' : 'Ask Agent',
      drawer: {
        title: isZh ? '项目分析助手' : 'Project Analyst',
        placeholder: isZh ? '问问这个项目的演进...' : 'Ask about this project\'s evolution...',
        minimize: isZh ? '缩小到角落' : 'Minimize',
      },
      chip: {
        1: isZh ? 'useStorage 主要由谁维护?' : 'Who maintains useStorage?',
        2: isZh ? 'composables/ 模块谁贡献最多?' : 'Who contributed most to composables/?',
        3: isZh ? '项目有哪些里程碑事件?' : 'What are milestone events?',
        4: isZh ? 'core 包近期最活跃?' : 'Which core package is most active?',
        5: isZh ? 'shared utils 最近被谁改动?' : 'Who changed shared utils?',
      },
      error: {
        noData: isZh ? '现有数据中查不到' : 'No data found',
        apiKeyMissing: isZh ? 'API Key 未配置' : 'API Key not configured',
        streamInterrupted: isZh ? '流中断，请重试' : 'Stream interrupted. Please retry.',
        rateLimited: isZh ? '请求过于频繁' : 'Rate limited',
        costCap: isZh ? '会话 token 接近上限' : 'Session token limit approaching',
        inputTooLong: isZh ? '500 / {n}' : '500 / {n}',
        aborted: isZh ? '已中断' : 'Aborted',
      },
      toolcard: {
        input: isZh ? '输入参数' : 'Input',
        output: isZh ? '返回结果' : 'Output',
        step: 'Step {n}',
        duration: '{n}ms',
      },
    },
    common: {
      cancel: isZh ? '取消' : 'Cancel',
      confirm: isZh ? '确认' : 'Confirm',
      retry: isZh ? '重试' : 'Retry',
    },
  }
}

function createI18nPlugin(locale: string) {
  return createI18n({
    legacy: false,
    locale,
    messages: {
      'zh-CN': makeMessages('zh-CN'),
      en: makeMessages('en'),
    },
  })
}

// Helper: build mock fetchEventSource that drives events via callbacks
function buildMockStream(handlers: {
  onopen?: (response: Response) => void | Promise<void>
  onmessage?: (event: { id: string; event: string; data: string }) => void
  onerror?: (err: Error) => void
}) {
  return async (_url: string, options: any) => {
    if (handlers.onopen) {
      const mockResponse = new Response(null, { status: 200 })
      await handlers.onopen(mockResponse)
    }
    if (handlers.onmessage) {
      // Allow test to trigger messages later via returned trigger function
    }
    if (handlers.onerror) {
      // Allow test to trigger error later
    }
    // Return a promise that never resolves (test controls via abort)
    return new Promise(() => {})
  }
}

describe('AgentChat', () => {
  beforeEach(() => {
    fetchEventSourceMock.mockReset()
  })

  afterEach(() => {
    vi.restoreAllMocks()
  })

  it('shows FAB and hides drawer initially (idle state)', () => {
    const wrapper = mount(AgentChat, {
      props: { projectId: 1 },
      global: { plugins: [createI18nPlugin('zh-CN')] },
    })

    // FAB visible
    const fab = wrapper.find('button[aria-label="问 Agent"]')
    expect(fab.exists()).toBe(true)
    expect(fab.isVisible()).toBe(true)

    // Drawer not visible (USlideover checks v-model:open)
    // USlideover renders but body may be hidden; check for drawer title
    expect(wrapper.text()).not.toContain('项目分析助手')
  })

  it('opens drawer when FAB is clicked', async () => {
    const wrapper = mount(AgentChat, {
      props: { projectId: 1 },
      global: { plugins: [createI18nPlugin('zh-CN')] },
    })

    await wrapper.find('button[aria-label="问 Agent"]').trigger('click')
    await nextTick()

    expect(wrapper.text()).toContain('项目分析助手')
  })

  it('minimizes drawer and preserves messages', async () => {
    const wrapper = mount(AgentChat, {
      props: { projectId: 1 },
      global: { plugins: [createI18nPlugin('zh-CN')] },
    })

    // Open drawer
    await wrapper.find('button[aria-label="问 Agent"]').trigger('click')
    await nextTick()

    // Type a message
    const input = wrapper.find('input')
    await input.setValue('test message')

    // Minimize
    await wrapper.find('button[aria-label="缩小到角落"]').trigger('click')
    await nextTick()

    // FAB should be visible again
    expect(wrapper.find('button[aria-label="问 Agent"]').isVisible()).toBe(true)

    // Re-open
    await wrapper.find('button[aria-label="问 Agent"]').trigger('click')
    await nextTick()

    // Input should still have the value
    expect((wrapper.find('input').element as HTMLInputElement).value).toBe('test message')
  })

  it('renders 5 chip questions in idle state', () => {
    const wrapper = mount(AgentChat, {
      props: { projectId: 1 },
      global: { plugins: [createI18nPlugin('zh-CN')] },
    })

    // Open drawer to see chips
    wrapper.find('button[aria-label="问 Agent"]').trigger('click')

    const chips = wrapper.findAll('button').filter(b =>
      b.text().includes('useStorage')
      || b.text().includes('composables')
      || b.text().includes('里程碑')
      || b.text().includes('core')
      || b.text().includes('shared'),
    )
    expect(chips.length).toBe(5)
  })

  it('clicking a chip fills input and triggers submit', async () => {
    fetchEventSourceMock.mockImplementation(async (_url, options) => {
      // Capture the body for assertion
      ;(globalThis as any).__lastAgentBody = options.body
      // Never resolve to keep stream open
      return new Promise(() => {})
    })

    const wrapper = mount(AgentChat, {
      props: { projectId: 1 },
      global: { plugins: [createI18nPlugin('zh-CN')] },
    })

    await wrapper.find('button[aria-label="问 Agent"]').trigger('click')
    await nextTick()

    // Find and click first chip
    const chip = wrapper.findAll('button').find(b => b.text().includes('useStorage'))
    expect(chip).toBeDefined()
    await chip!.trigger('click')
    await flushPromises()

    // Verify fetchEventSource was called
    expect(fetchEventSourceMock).toHaveBeenCalledTimes(1)

    // Verify body contains chip text
    const body = JSON.parse((globalThis as any).__lastAgentBody)
    expect(body.message).toContain('useStorage')
  })

  it('disables submit when input is too long (>500 chars)', async () => {
    const wrapper = mount(AgentChat, {
      props: { projectId: 1 },
      global: { plugins: [createI18nPlugin('zh-CN')] },
    })

    await wrapper.find('button[aria-label="问 Agent"]').trigger('click')
    await nextTick()

    const input = wrapper.find('input')
    await input.setValue('a'.repeat(501))
    await nextTick()

    const submitBtn = wrapper.find('button[aria-label="确认"]')
    expect(submitBtn.attributes('disabled')).toBeDefined()

    // Counter should show red text
    expect(wrapper.text()).toContain('500 / 501')
  })

  it('shows api-key-missing overlay on 503', async () => {
    fetchEventSourceMock.mockImplementation(async (_url, options) => {
      const mockResponse = new Response(null, { status: 503 })
      await options.onopen(mockResponse)
      return new Promise(() => {})
    })

    const wrapper = mount(AgentChat, {
      props: { projectId: 1 },
      global: { plugins: [createI18nPlugin('zh-CN')] },
    })

    await wrapper.find('button[aria-label="问 Agent"]').trigger('click')
    await nextTick()

    // Submit a message to trigger the stream
    const input = wrapper.find('input')
    await input.setValue('test')
    await wrapper.find('button[aria-label="确认"]').trigger('click')
    await flushPromises()

    expect(wrapper.text()).toContain('API Key 未配置')
  })

  it('switches locale from zh-CN to en', async () => {
    const i18n = createI18nPlugin('zh-CN')
    const wrapper = mount(AgentChat, {
      props: { projectId: 1 },
      global: { plugins: [i18n] },
    })

    await wrapper.find('button[aria-label="问 Agent"]').trigger('click')
    await nextTick()

    expect(wrapper.text()).toContain('项目分析助手')

    // Switch locale
    i18n.global.locale.value = 'en'
    await nextTick()

    expect(wrapper.text()).toContain('Project Analyst')
    expect(wrapper.text()).toContain('Ask Agent')
  })

  it('shows abort button during streaming and calls abort', async () => {
    fetchEventSourceMock.mockImplementation(async (_url, options) => {
      const mockResponse = new Response(null, { status: 200 })
      await options.onopen(mockResponse)
      // Send a text event
      options.onmessage({ id: '', event: 'message', data: JSON.stringify({ type: 'text', token: 'hello' }) })
      return new Promise(() => {})
    })

    const wrapper = mount(AgentChat, {
      props: { projectId: 1 },
      global: { plugins: [createI18nPlugin('zh-CN')] },
    })

    await wrapper.find('button[aria-label="问 Agent"]').trigger('click')
    await nextTick()

    const input = wrapper.find('input')
    await input.setValue('test')
    await wrapper.find('button[aria-label="确认"]').trigger('click')
    await flushPromises()

    // Stop button should appear (square icon)
    const stopBtn = wrapper.find('button[aria-label="取消"]')
    expect(stopBtn.exists()).toBe(true)

    // Click stop
    await stopBtn.trigger('click')
    expect(wrapper.text()).toContain('已中断')
  })
})
```

**注意事项：**
- `vi.doMock` 必须在动态 `import()` 之前
- `flushPromises` 用于等待 Vue 的异步更新队列
- `nextTick` 用于等待 DOM 更新
- USlideover 的 `v-model:open` 在测试中通过点击 FAB/minimize 按钮触发
- fetchEventSource mock 返回永不 resolve 的 Promise，测试通过 abort/stop 控制结束
- `globalThis.__lastAgentBody` 用于捕获 POST body 做断言（hacky but effective for component tests）
- 如果 USlideover 在 jsdom 中渲染异常，可以用 `stubs: { USlideover: false }` 保留真实渲染
  </action>
  <acceptance_criteria>
    - 文件 `apps/web/test/components/AgentChat.spec.ts` 存在
    - `grep "@vitest-environment jsdom" apps/web/test/components/AgentChat.spec.ts` 返回匹配
    - `grep "vi.doMock.*fetch-event-source" apps/web/test/components/AgentChat.spec.ts` 返回匹配
    - `grep "mount.*AgentChat" apps/web/test/components/AgentChat.spec.ts` 返回匹配
    - `grep "createI18n" apps/web/test/components/AgentChat.spec.ts` 返回匹配
    - `grep "问 Agent" apps/web/test/components/AgentChat.spec.ts` 返回匹配
    - `grep "项目分析助手" apps/web/test/components/AgentChat.spec.ts` 返回匹配
    - `grep "useStorage" apps/web/test/components/AgentChat.spec.ts` 返回匹配
    - `grep "500 / 501" apps/web/test/components/AgentChat.spec.ts` 返回匹配
    - `grep "API Key 未配置" apps/web/test/components/AgentChat.spec.ts` 返回匹配
    - `grep "Project Analyst" apps/web/test/components/AgentChat.spec.ts` 返回匹配
    - `grep "已中断" apps/web/test/components/AgentChat.spec.ts` 返回匹配
    - `grep -c "it(" apps/web/test/components/AgentChat.spec.ts` 返回 ">= 10"
    - `pnpm --filter web exec vitest run test/components/AgentChat.spec.ts` 全部 PASS
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/lionad/Github/Lionad-Morotar/project-river && pnpm --filter web exec vitest run test/components/AgentChat.spec.ts 2>&1 | tail -30</automated>
  </verify>
  <done>
    >= 10 个 it() 测试全部 pass
    覆盖：idle/streaming/tool-calling/stream-mid-error/abort/rate-limit/cost-cap/input-too-long/api-key-missing/empty-result 状态
    覆盖：FAB/drawer 切换、chip 点击、i18n 切换、input 长度限制
    无真实 SSE 或 LLM 调用
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test runner → Component | Tests mount production components; mock layer prevents external IO |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-T01 | T | AgentChat.spec.ts | mitigate | vi.doMock isolates fetchEventSource — no real HTTP calls |
| T-04-T02 | T | AgentToolCard.spec.ts | mitigate | Pure component test, no external dependencies |
| T-04-T03 | I | both spec files | accept | Test fixtures contain no real API keys |
</threat_model>

<verification>
1. `pnpm --filter web exec vitest run test/components/AgentChat.spec.ts` 全部 PASS
2. `pnpm --filter web exec vitest run test/components/AgentToolCard.spec.ts` 全部 PASS
3. `pnpm --filter web exec vitest run test/components/` 全部 PASS（含既有 ResizeHandle.spec.ts）
4. `pnpm --filter web exec tsc --noEmit` 通过
5. >= 17 个测试用例（AgentChat >= 10 + AgentToolCard 7）全部独立通过
</verification>

<success_criteria>
- AgentChat.spec.ts: >= 10 个测试覆盖所有 UI States + FAB/drawer + chip + i18n + input 限制
- AgentToolCard.spec.ts: 7 个测试覆盖折叠/展开/JSON/isError/running/i18n
- 全部测试 pass
- D-15/D-16 的 mock 模式严格执行
- 无真实 SSE 或 LLM 调用
</success_criteria>

<output>
After completion, create `.planning/phases/04-ui-i18n-chat-surface/04-02-SUMMARY.md`
</output>
